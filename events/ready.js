const { Events, ActivityType } = require('discord.js');

module.exports = {
	name: Events.ClientReady,
	once: true,
	execute(client) {
		console.log(`Ready! Logged in as ${client.user.tag}`);
		client.user.setActivity('all the bitches at the National Treasury print my face on $10 bills', { type: ActivityType.Watching });
	},
};